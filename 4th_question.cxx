#include<iostream>
using namespace std;
int main()
{ 
/*logic 
int top = 0, bottom = n - 1, left = 0, right = n - 1;
    int num = 1;

           for (int i = left; i <= right; i++)  // Move right

            for (int i = top; i <= bottom; i++)  // Move down
    
            for (int i = right; i >= left; i--)  // Move left
    
            for (int i = bottom; i >= top; i--)  // Move up
                */
}